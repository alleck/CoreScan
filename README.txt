===================
= CoreScan README =
===================

Usage steps:

	Pretty simple - Just run the corescan.sh file with "sh corescan.sh". It knows where to look.
	
The script will REMOVE all the core dump files it finds.